#include <stdio.h>



// syntax
// (type) value;

int main()
{   
    
    int a = 5;
    float b = (float)68/5;
    // float before 68 is used to conver intger 68 into float so it gives decimal value when divided by integer 5
    
    printf("Value is %f\n", b);
    return 0;
}

